input{
    rabbitmq{
    host=>"rabbitmq"                      
    port=> 5672                            
    user=>"user"                           
    password=>"password"                   
    queue=>"logstash-rabbitmq"             
    durable=> true                         
    codec=>json                            
    } 
}

output {
  elasticsearch {
        hosts => ["elasticsearch:9200"]
        index => "logstash-rabbitmq"
        user => "elastic"
        password => "admin888"
    }
}
